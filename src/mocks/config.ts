// ─── Mock API Configuration ───────────────────────────────────────────────────
// Set USE_MOCK_API = true  → app uses local mock data (backend not required)
// Set USE_MOCK_API = false → app calls the real backend API
// TODO: Set to false once the backend is fully implemented and tested.

export const USE_MOCK_API = true;

// Simulated network delay in milliseconds (makes mock feel realistic)
export const MOCK_DELAY_MS = 800;
